# Docker-Assignment-
Installed Docker and verified functionality. Created a sample Node.js application and wrote a Dockerfile to containerize it using an official Node base image. Built and ran the Docker image as a container, exposed required ports, optimized image size with Alpine, and pushed the image to Docker Hub for deployment and portability.
